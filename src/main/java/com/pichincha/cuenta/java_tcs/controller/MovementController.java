package com.pichincha.cuenta.java_tcs.controller;

import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/v1")
public class MovementController {
/*

    @Autowired
    private IAccountMovementService iAccountMovementService;


    @RequestMapping(value = "/saveMovement",method = RequestMethod.PUT, consumes = MediaType.APPLICATION_JSON_VALUE)
    @ResponseBody
    public Account saveMovement(@RequestBody(required = false) AccountMovement accountMovement) throws Exception {
        return accountMovementService.save(accountMovement);
    }*/
}
